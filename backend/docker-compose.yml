services:

  backend:

    build:
      context: .

    container_name: fridge-monitor-api

    ports:
      - "8000:8000"

    env_file:
      - .env

    volumes:
      - ./data:/app/data

    restart: unless-stopped